using static System.Console;
WriteLine("* Top-level functions example");
// signatures des fonctions en bas du code
// aussi les types et les classes : préférables dans un autre fichier

WhatsMyNamespace(); // Call the function.
